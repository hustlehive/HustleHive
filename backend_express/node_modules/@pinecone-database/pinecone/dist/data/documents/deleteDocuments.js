"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteDocuments = void 0;
const db_data_1 = require("../../pinecone-generated-ts-fetch/db_data");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
const documentValidation_1 = require("./documentValidation");
const deleteDocuments = async (api, namespace, options) => {
    const given = [
        options.ids !== undefined,
        options.filter !== undefined,
        !!options.deleteAll,
    ].filter(Boolean).length;
    if (given === 0) {
        throw new errors_1.PineconeArgumentError('You must specify exactly one of `ids`, `filter`, or `deleteAll` to deleteDocuments.');
    }
    if (given > 1) {
        throw new errors_1.PineconeArgumentError('`ids`, `filter`, and `deleteAll` are mutually exclusive in deleteDocuments; pass exactly one.');
    }
    (0, documentValidation_1.assertNonEmptyArray)(options.ids, 'ids', 'document ID', 'deleteDocuments');
    try {
        return await api.deleteDocuments({
            namespace,
            deleteDocumentsRequest: options,
            xPineconeApiVersion: db_data_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, msg) => `Error deleting documents from namespace ${namespace}: ${msg}`);
    }
};
exports.deleteDocuments = deleteDocuments;
//# sourceMappingURL=deleteDocuments.js.map