"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.assertNonEmptyArray = void 0;
const errors_1 = require("../../errors");
/**
 * Rejects an array argument that is present but empty.
 *
 * The document request models grant "empty means no change, so it is ignored"
 * only to `set_fields` and `remove_fields`. Every other array argument counts
 * as specified once it is present, so an empty one is a caller mistake rather
 * than an absent argument.
 */
const assertNonEmptyArray = (values, argumentName, entryDescription, methodName) => {
    if (values !== undefined && values.length === 0) {
        throw new errors_1.PineconeArgumentError(`\`${argumentName}\` must contain at least one ${entryDescription} in ${methodName}.`);
    }
};
exports.assertNonEmptyArray = assertNonEmptyArray;
//# sourceMappingURL=documentValidation.js.map