import { VectorOperationsProvider } from './vectorOperationsProvider';
import { IntegratedRecord, PineconeConfiguration, RecordMetadata } from './types';
import { HTTPHeaders } from '../../pinecone-generated-ts-fetch/db_data';
/**
 * Options for upserting integrated records into a namespace.
 *
 * @see {@link Index.upsertRecords}
 */
export type UpsertRecordsOptions<T extends RecordMetadata = RecordMetadata> = {
    /** The integrated records to upsert */
    records: Array<IntegratedRecord<T>>;
    /**
     * The namespace to upsert into. If not specified, uses the namespace configured on the Index.
     */
    namespace?: string;
};
export declare class UpsertRecordsCommand<T extends RecordMetadata = RecordMetadata> {
    apiProvider: VectorOperationsProvider;
    config: PineconeConfiguration;
    namespace: string;
    additionalHeaders?: HTTPHeaders;
    constructor(apiProvider: VectorOperationsProvider, namespace: string, config: PineconeConfiguration, additionalHeaders?: HTTPHeaders);
    validator: (options: UpsertRecordsOptions<T>) => void;
    run(options: UpsertRecordsOptions<T>): Promise<void>;
}
