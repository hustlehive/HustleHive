import type { ManageIndexesApi, BackupModel as GeneratedBackupModel } from '../../pinecone-generated-ts-fetch/db_control';
import type { BackupStatus } from '../types';
import type { IndexSchema } from '../indexes/listIndexes';
/**
 * The configuration and status of a backup.
 *
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
export interface BackupModel extends Omit<GeneratedBackupModel, 'status' | 'schema'> {
    /** The current status of the backup. */
    status: BackupStatus;
    /** The schema of the index the backup was taken from. */
    schema?: IndexSchema;
}
/**
 * Retrieves the configuration and status of a Pinecone backup.
 *
 * @param api - The manage-indexes API client.
 * @param backupId - The ID of the backup to describe.
 * @returns A promise that resolves to the BackupModel for the specified backup.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
export declare function describeBackup(api: ManageIndexesApi, backupId: string): Promise<BackupModel>;
