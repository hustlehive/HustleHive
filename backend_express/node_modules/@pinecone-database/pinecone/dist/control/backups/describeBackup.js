"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.describeBackup = describeBackup;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Retrieves the configuration and status of a Pinecone backup.
 *
 * @param api - The manage-indexes API client.
 * @param backupId - The ID of the backup to describe.
 * @returns A promise that resolves to the BackupModel for the specified backup.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
async function describeBackup(api, backupId) {
    if (!backupId) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `backupId` in order to describe a backup.');
    }
    try {
        return await api.describeBackup({
            backupId,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error describing backup ${backupId}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=describeBackup.js.map