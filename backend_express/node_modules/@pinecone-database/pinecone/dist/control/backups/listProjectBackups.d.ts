import type { ManageIndexesApi } from '../../pinecone-generated-ts-fetch/db_control';
import type { BackupList } from './listIndexBackups';
/**
 * Options for listing all backups in a project.
 *
 */
export interface ListProjectBackupsOptions {
    /** Maximum number of results per page. Omit to use the service default. */
    limit?: number;
    /** Pagination token from a prior response to retrieve the next page. */
    paginationToken?: string;
}
/**
 * Lists all backups across every index in the project.
 *
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
export declare function listProjectBackups(api: ManageIndexesApi, options?: ListProjectBackupsOptions): Promise<BackupList>;
