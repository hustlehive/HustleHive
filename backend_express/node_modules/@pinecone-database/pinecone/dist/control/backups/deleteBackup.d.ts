import type { ManageIndexesApi } from '../../pinecone-generated-ts-fetch/db_control';
/**
 * Deletes a Pinecone backup.
 *
 * The delete is accepted asynchronously — the backup may not be removed immediately.
 * Returns once the delete request is accepted (HTTP 202).
 *
 * @param api - The manage-indexes API client.
 * @param backupId - The ID of the backup to delete.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
export declare const deleteBackup: (api: ManageIndexesApi, backupId: string) => Promise<void>;
