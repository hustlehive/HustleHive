"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteBackup = void 0;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Deletes a Pinecone backup.
 *
 * The delete is accepted asynchronously — the backup may not be removed immediately.
 * Returns once the delete request is accepted (HTTP 202).
 *
 * @param api - The manage-indexes API client.
 * @param backupId - The ID of the backup to delete.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
const deleteBackup = async (api, backupId) => {
    if (!backupId) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `backupId` in order to delete a backup.');
    }
    try {
        await api.deleteBackup({
            backupId,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error deleting backup ${backupId}: ${rawMessageText}`);
    }
};
exports.deleteBackup = deleteBackup;
//# sourceMappingURL=deleteBackup.js.map