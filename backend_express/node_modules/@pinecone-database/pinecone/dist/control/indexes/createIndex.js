"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createIndex = createIndex;
const decorateIndexModel_1 = require("./decorateIndexModel");
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
const utils_1 = require("../../utils");
const legacyTranslation_1 = require("./legacyTranslation");
async function createIndex(api, options) {
    const normalized = (0, legacyTranslation_1.translateLegacyCreateOptions)(options);
    if (!normalized.name) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to create an index.');
    }
    if (!normalized.schema) {
        throw new errors_1.PineconeArgumentError('You must pass a `schema` object in order to create an index.');
    }
    const { waitUntilReady, timeout, suppressConflicts, ...createRequest } = normalized;
    try {
        const result = await api.createIndex({
            createIndexRequest: createRequest,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
        if (waitUntilReady) {
            return await (0, utils_1.pollUntilIndexIsReady)(async () => {
                try {
                    return (0, decorateIndexModel_1.decorateIndexModel)(await api.describeIndex({
                        indexName: options.name,
                        xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
                    }));
                }
                catch (e) {
                    throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error waiting for index ${options.name} to be ready: ${rawMessageText}`);
                }
            }, options.name, timeout);
        }
        return (0, decorateIndexModel_1.decorateIndexModel)(result);
    }
    catch (e) {
        if (suppressConflicts &&
            e instanceof Error &&
            e.name === 'PineconeConflictError') {
            return;
        }
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error creating index ${options.name}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=createIndex.js.map