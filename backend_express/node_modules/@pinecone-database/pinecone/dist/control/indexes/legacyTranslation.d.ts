import type { ReadCapacity } from '../types';
import type { IndexDeploymentRequest, PatchIndexDeploymentRequest } from '../../pinecone-generated-ts-fetch/db_control';
import type { CreateIndexOptions, NativeCreateIndexOptions, CreateIndexSchema } from './createIndex';
import type { ConfigureIndexResourceOptions, NativeConfigureIndexOptions } from './configureIndex';
/** Translate a legacy deployment without performing a control-plane request. */
export declare function specToDeployment(spec: unknown): IndexDeploymentRequest;
/** Normalize legacy flat capacity while preserving native API settings. */
export declare function normalizeReadCapacity(value: unknown): ReadCapacity | undefined;
/** Lift and copy read capacity from the legacy deployment spec. */
export declare function specToReadCapacity(spec: unknown): ReadCapacity | undefined;
/** Construct exactly one reserved vector field for the vectors API. */
export declare function legacyVectorSchema(args: {
    dimension?: number;
    metric?: string;
    vectorType?: string;
}): CreateIndexSchema;
/** Translate legacy pod scaling into a sparse PATCH deployment. */
export declare function legacyPodScaling(args: {
    podReplicas?: number;
    podType?: string;
}): PatchIndexDeploymentRequest;
/** Normalize either public create shape without changing the caller's object. */
export declare function translateLegacyCreateOptions(options: CreateIndexOptions): NativeCreateIndexOptions;
/** Normalize legacy pod scaling and flat read capacity without a describe call. */
export declare function translateLegacyConfigureOptions(options: ConfigureIndexResourceOptions): NativeConfigureIndexOptions;
