"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.describeIndex = describeIndex;
const decorateIndexModel_1 = require("./decorateIndexModel");
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Describes an index by name.
 *
 */
async function describeIndex(api, indexName) {
    if (!indexName) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `indexName` in order to describe an index.');
    }
    try {
        return (0, decorateIndexModel_1.decorateIndexModel)(await api.describeIndex({
            indexName,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        }));
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error describing index ${indexName}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=describeIndex.js.map