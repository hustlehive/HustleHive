"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decorateIndexModel = decorateIndexModel;
exports.decorateIndexList = decorateIndexList;
const legacyAccessors_1 = require("./legacyAccessors");
const indexProperty_1 = require("../../errors/indexProperty");
function read(result) {
    if (result.outcome === 'error')
        throw new indexProperty_1.PineconeIndexPropertyError(result.failure);
    return result.outcome === 'value' ? result.value : undefined;
}
/** Install lazy, non-enumerable legacy accessors on a plain API response. */
function decorateIndexModel(model) {
    const getters = {
        dimension: () => read((0, legacyAccessors_1.deriveDimension)(model)),
        metric: () => read((0, legacyAccessors_1.deriveMetric)(model)),
        vectorType: () => read((0, legacyAccessors_1.deriveVectorType)(model)),
        embed: () => read((0, legacyAccessors_1.deriveEmbed)(model)),
        spec: () => {
            const spec = read((0, legacyAccessors_1.deriveSpec)(model));
            const variant = spec.serverless ?? spec.byoc;
            if (variant && !model.readCapacity)
                Object.defineProperty(variant, 'readCapacity', {
                    configurable: true,
                    enumerable: false,
                    get: () => read((0, legacyAccessors_1.deriveLegacyReadCapacity)(model)),
                });
            return spec;
        },
    };
    for (const [property, get] of Object.entries(getters)) {
        // Preserve already-decorated objects, including a cached response instance.
        if (!Object.getOwnPropertyDescriptor(model, property)?.get)
            Object.defineProperty(model, property, {
                configurable: true,
                enumerable: false,
                get,
            });
    }
    return model;
}
/** Decorate every index independently without changing the list envelope. */
function decorateIndexList(list) {
    list.indexes?.forEach(decorateIndexModel);
    return list;
}
//# sourceMappingURL=decorateIndexModel.js.map