"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.configureIndex = configureIndex;
const decorateIndexModel_1 = require("./decorateIndexModel");
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
const legacyTranslation_1 = require("./legacyTranslation");
/**
 * Configures an index.
 *
 * Only the fields present in `options` are updated; omit a field to leave it unchanged.
 *
 */
async function configureIndex(api, name, options) {
    if (!name) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to configure an index.');
    }
    const normalized = (0, legacyTranslation_1.translateLegacyConfigureOptions)(options);
    const fields = [
        'deployment',
        'schema',
        'readCapacity',
        'tags',
        'deletionProtection',
    ];
    if (!options ||
        !fields.some((field) => normalized[field] !== undefined)) {
        throw new errors_1.PineconeArgumentError('You must pass at least one configuration option to configureIndex.');
    }
    try {
        return (0, decorateIndexModel_1.decorateIndexModel)(await api.configureIndex({
            indexName: name,
            configureIndexRequest: normalized,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        }));
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error configuring index ${name}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=configureIndex.js.map