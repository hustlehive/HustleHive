"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createIndexForModel = createIndexForModel;
const decorateIndexModel_1 = require("./decorateIndexModel");
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
const utils_1 = require("../../utils");
const legacyTranslation_1 = require("./legacyTranslation");
async function createIndexForModel(api, options) {
    if (!options) {
        throw new errors_1.PineconeArgumentError('You must pass an object with required properties (`name`, `cloud`, `region`, `embed`) to create an index for a model.');
    }
    if (!options.name) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to create an index.');
    }
    if (!options.cloud) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `cloud` in order to create an index.');
    }
    if (!options.region) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `region` in order to create an index.');
    }
    if (!options.embed) {
        throw new errors_1.PineconeArgumentError('You must pass an `embed` object in order to create an index for a model.');
    }
    if (!options.embed.model) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `embed.model` in order to create an index for a model.');
    }
    if (!options.embed.fieldMap ||
        Object.keys(options.embed.fieldMap).length === 0) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty `embed.fieldMap` object in order to create an index for a model.');
    }
    if (options.embed.metric &&
        !['cosine', 'euclidean', 'dotproduct'].includes(options.embed.metric.toLowerCase())) {
        throw new errors_1.PineconeArgumentError(`Invalid metric value: ${options.embed.metric}. Valid values are: cosine, euclidean, or dotproduct.`);
    }
    const { waitUntilReady, timeout, suppressConflicts, readCapacity, ...rest } = options;
    const createRequest = {
        ...rest,
        readCapacity: (0, legacyTranslation_1.normalizeReadCapacity)(readCapacity),
    };
    try {
        const result = await api.createIndexForModel({
            createIndexForModelRequest: createRequest,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
        if (waitUntilReady) {
            // A missing response name must not turn a successful create into an
            // invalid describe request. The input name has already been validated.
            const indexName = typeof result.name === 'string' && result.name.length > 0
                ? result.name
                : options.name;
            return await (0, utils_1.pollUntilIndexIsReady)(async () => {
                try {
                    return (0, decorateIndexModel_1.decorateIndexModel)(await api.describeIndex({
                        indexName,
                        xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
                    }));
                }
                catch (e) {
                    throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error waiting for index ${indexName} to be ready: ${rawMessageText}`);
                }
            }, indexName, timeout);
        }
        return (0, decorateIndexModel_1.decorateIndexModel)(result);
    }
    catch (e) {
        if (suppressConflicts &&
            e instanceof Error &&
            e.name === 'PineconeConflictError') {
            return;
        }
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error creating index for model ${options.name}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=createIndexForModel.js.map