"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteBackupSchedule = deleteBackupSchedule;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Deletes a backup schedule. Backups it already produced are kept until their
 * own retention window ends.
 *
 * @param api - The manage-indexes API client.
 * @param scheduleId - The ID of the schedule to delete.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
async function deleteBackupSchedule(api, scheduleId) {
    if (!scheduleId) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `scheduleId` in order to delete a backup schedule.');
    }
    try {
        await api.deleteBackupSchedule({
            scheduleId,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error deleting backup schedule ${scheduleId}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=deleteBackupSchedule.js.map