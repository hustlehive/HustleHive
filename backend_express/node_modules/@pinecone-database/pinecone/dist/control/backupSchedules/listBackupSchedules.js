"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.listBackupSchedules = listBackupSchedules;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Lists the backup schedules attached to an index.
 *
 * @param api - The manage-indexes API client.
 * @param indexName - Name of the index whose schedules to list.
 * @param options - Optional pagination parameters.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
async function listBackupSchedules(api, indexName, options = {}) {
    if (!indexName) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `indexName` in order to list backup schedules.');
    }
    try {
        return await api.listBackupSchedules({
            indexName,
            limit: options.limit,
            paginationToken: options.paginationToken,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error listing backup schedules for index ${indexName}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=listBackupSchedules.js.map