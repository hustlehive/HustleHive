"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.describeBackupSchedule = describeBackupSchedule;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Retrieves the configuration and status of a backup schedule.
 *
 * @param api - The manage-indexes API client.
 * @param scheduleId - The ID of the schedule to describe.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
async function describeBackupSchedule(api, scheduleId) {
    if (!scheduleId) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `scheduleId` in order to describe a backup schedule.');
    }
    try {
        return await api.describeBackupSchedule({
            scheduleId,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error describing backup schedule ${scheduleId}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=describeBackupSchedule.js.map