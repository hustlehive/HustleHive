"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateBackupSchedule = updateBackupSchedule;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Updates the cadence, retention, or enabled state of a backup schedule.
 *
 * @param api - The manage-indexes API client.
 * @param scheduleId - The ID of the schedule to update.
 * @param options - The fields to change.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
async function updateBackupSchedule(api, scheduleId, options = {}) {
    if (!scheduleId) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `scheduleId` in order to update a backup schedule.');
    }
    const updateBackupScheduleRequest = {};
    if (options.frequency !== undefined) {
        updateBackupScheduleRequest.frequency = options.frequency;
    }
    if (options.retentionDays !== undefined) {
        updateBackupScheduleRequest.retention = {
            expireAfterDays: options.retentionDays,
        };
    }
    if (options.enabled !== undefined) {
        updateBackupScheduleRequest.enabled = options.enabled;
    }
    try {
        return await api.updateBackupSchedule({
            scheduleId,
            updateBackupScheduleRequest,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error updating backup schedule ${scheduleId}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=updateBackupSchedule.js.map