"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.listBackupScheduleHistory = listBackupScheduleHistory;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Lists the backups a schedule has produced or planned.
 *
 * @param api - The manage-indexes API client.
 * @param scheduleId - The ID of the schedule whose history to list.
 * @param options - Optional pagination parameters.
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 */
async function listBackupScheduleHistory(api, scheduleId, options = {}) {
    if (!scheduleId) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `scheduleId` in order to list backup schedule history.');
    }
    try {
        return await api.listBackupScheduleHistory({
            scheduleId,
            limit: options.limit,
            paginationToken: options.paginationToken,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error listing history for backup schedule ${scheduleId}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=listBackupScheduleHistory.js.map