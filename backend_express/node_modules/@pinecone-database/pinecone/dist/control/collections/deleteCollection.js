"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteCollection = deleteCollection;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Deletes an existing collection.
 *
 * @see [Collections](https://docs.pinecone.io/guides/indexes/collections)
 */
async function deleteCollection(api, collectionName) {
    if (!collectionName) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `collectionName` in order to delete a collection.');
    }
    try {
        await api.deleteCollection({
            collectionName,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error deleting collection ${collectionName}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=deleteCollection.js.map