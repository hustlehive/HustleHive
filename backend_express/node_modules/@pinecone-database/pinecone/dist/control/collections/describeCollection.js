"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.describeCollection = describeCollection;
const db_control_1 = require("../../pinecone-generated-ts-fetch/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Retrieves metadata for a single named collection.
 *
 * Collections are only supported for pod-based indexes; serverless indexes do
 * not support collections.
 *
 * @param api - The manage-indexes API client.
 * @param collectionName - The name of the collection to describe.
 * @see [Collections](https://docs.pinecone.io/guides/indexes/collections/understanding-collections)
 */
async function describeCollection(api, collectionName) {
    if (!collectionName) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `collectionName` in order to describe a collection.');
    }
    try {
        return await api.describeCollection({
            collectionName,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error describing collection ${collectionName}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=describeCollection.js.map