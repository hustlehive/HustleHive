import { InferenceApi, RerankResult } from '../pinecone-generated-ts-fetch/inference';
/**
 * Options for reranking documents against a query.
 *
 * @example
 * ```typescript
 * import type { RerankOptions } from '@pinecone-database/pinecone';
 * const options: RerankOptions = {
 *   model: 'bge-reranker-v2-m3',
 *   query: 'How do I return an order?',
 *   documents: [{ body: 'Start a return from your order history.' }],
 *   rankFields: ['body'],
 * };
 * ```
 */
export interface RerankOptions {
    /**
     * The [model](https://docs.pinecone.io/guides/search/rerank-results#reranking-models) to use for reranking.
     */
    model: string;
    /**
     * The query to rerank documents against.
     */
    query: string;
    /**
     * Text strings or document objects. Objects need a `text` field unless you supply `rankFields`.
     */
    documents: Array<{
        [key: string]: string;
    } | string>;
    /**
     * The number of results to return sorted by relevance. Defaults to the number of inputs.
     */
    topN?: number;
    /**
     * Include document contents in each result. Defaults to `true`.
     */
    returnDocuments?: boolean;
    /**
     * The field(s) to consider for reranking. If not provided, the default is `["text"]`.
     *
     * The number of fields supported is [model-specific](https://docs.pinecone.io/guides/search/rerank-results#reranking-models).
     */
    rankFields?: Array<string>;
    /**
     * Additional model-specific parameters. Refer to the [model guide](https://docs.pinecone.io/guides/search/rerank-results#reranking-models) for available model parameters.
     */
    parameters?: {
        [key: string]: string;
    };
}
export declare const rerank: (infApi: InferenceApi) => (options: RerankOptions) => Promise<RerankResult>;
